import { dataBaseType } from '@/types/dataBase'

export interface consolidationItemInterface extends dataBaseType {
  plantCd: stringNull
  saiJointInspectionItemId: stringNull
  jointItemName: stringNull
  useFlag: stringNull
  sortOrder: stringNull
}
